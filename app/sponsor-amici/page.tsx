import { getPublicSnapshot } from "@/lib/nac-data";
export const dynamic="force-dynamic";
export default async function SponsorPage(){const data=await getPublicSnapshot();return <main className="public-page"><header className="public-page-head"><nav><a className="brand" href="/"><img src="/nac-scudetto.png" alt="Scudetto NAC"/><span>NAC<br/><i>AMATORI</i></span></a><div className="links"><a href="/la-storia">STORIA</a><a href="/squadra">SQUADRA</a><a href="/dirigenti-staff">STAFF</a><a href="/calendario">CALENDARIO</a><a href="/classifica">CLASSIFICA</a><a href="/sponsor-amici">SPONSOR</a><a href="/tifosi-amici">TIFOSI</a></div></nav><div className="public-page-title"><small>PARTNER</small><h1>Sponsor & Amici.</h1><p>{data.settings.partner_text||"Le aziende e gli amici che scelgono di sostenere NAC condividono la nostra idea di sport e territorio."}</p></div></header><div className="public-page-body">{data.sponsors.length?<div className="sponsor-grid">{data.sponsors.map(s=><a className="sponsor-card" href={s.website||"#"} key={s.id}>{s.logoKey&&<img src={`/api/media/${s.logoKey}`} alt={s.name}/>}<h3>{s.name}</h3></a>)}</div>:<div className="empty-note">La sezione sponsor è pronta per essere popolata dall’Admin.</div>}<div className="page-actions"><a className="button" href="mailto:info@nacamatori.it">DIVENTA SPONSOR →</a></div></div></main>}
